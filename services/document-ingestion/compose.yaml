services:
  document-ingestion:
    build: .
    ports:
      - "8000:8000"
    env_file: .env.example
    volumes:
      - ./data:/data
    networks:
      - app_network

networks:
  app_network:
    driver: bridge