services:
  chat-qa:
    build: .
    ports:
      - "8080:8080"
    env_file: .env.example
    volumes:
      - ./data:/data
    networks:
      - app_network

networks:
  app_network:
    driver: bridge