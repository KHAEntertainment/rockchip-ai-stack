# -----------------------------------------------------------------------
# Base requirements shared across all RK3588 port components.
# Each component's requirements.txt should include:
#   -r ../shared/requirements-base.txt
# and then add component-specific packages below that line.
# -----------------------------------------------------------------------

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Settings management
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Vector database (replaces PGVector)
lancedb>=0.9.0
pyarrow>=15.0.0

# Numeric
numpy>=1.26.0

# LangChain stack (OpenAI-compatible clients + text splitters)
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# HTTP clients
httpx>=0.27.0
aiohttp>=3.9.0
